<% if grade == nil then grade = tonumber(string.sub(collection.name, 6)) end %>
<div class="c-puzzles-grid">
<%
for idx, puzzle in pairs(puzzles) do
	render(
		'frontend.views.partials.card-puzzle',
		{
			grade = grade,
			puzzle = puzzle
		}
	)
end
%>
</div>
