<div class="c-puzzles-grid">

<%
if (grade == null) then
	grade = schule_utils:grade_from_collection(collection)
end

for idx, puzzle in pairs(puzzles) do
	render(
		'frontend.views.partials.card-puzzle',
		{
			grade = grade,
			puzzle = puzzle
		}
	)
end
%>

</div>
